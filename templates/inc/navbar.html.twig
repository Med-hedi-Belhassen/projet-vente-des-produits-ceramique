 <style>
/* Dropdown Button */



.dropbtn {
  background-color: #00000000;
  color: white;
  padding: 16px;
  font-size: 16px;
  border: none;
}
.dropbtn1 {
  
  padding: 16px;
  font-size: 16px;
  border: none;
}
/* The container <div> - needed to position the dropdown content */
.dropdown {
  position: relative;
  display: inline-block;
}

/* Dropdown Content (Hidden by Default) */
.dropdown-content {
  display: none;
  position: absolute;
  background-color: #f1f1f1;
  min-width: 160px;
  box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
  z-index: 1;
}

/* Links inside the dropdown */
.dropdown-content a {
  color: black;
  padding: 12px 16px;
  text-decoration: none;
  display: block;
}

/* Change color of dropdown links on hover */
.dropdown-content a:hover {background-color: #ddd;}

/* Show the dropdown menu on hover */
.dropdown:hover .dropdown-content {display: block;}

/* Change the background color of the dropdown button when the dropdown content is shown */
.dropdown:hover .dropbtn {background-color: #00000000;}
</style>
  {% if app.user %}
   <nav class="navbar navbar-expand-sm navbar-dark bg-primary mb-3">
    
    <div class="container">
     <img src="{{ asset('/uploads/test1.jpg') }}"  class="rounded-circle" width="100" hieght=-"100">
        <a href="/" class="navbar-brand">Céramique art daily</a>
       <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobile-nav">
        <span class="navbar-toggle-icon"></span>
        </button>
         <ul class="navbar-nav ml-auto"> 

          <li class="nav-item">
             <div class="dropdown">
               <button class="dropbtn">produit</i></button>
                    <div class="dropdown-content">
                         <a href="{{ path('produit_index') }}" >Liste des produit</a>
                         {% if is_granted('ROLE_EDITOR') %}
                         <a href="{{ path('produit_new') }}" >Ajouter un produit</a>
                         {% endif %} 
                      </div>
             </div>
        
          </li>
            <li class="nav-item">
          <div class="dropdown">
               <button class="dropbtn">Matiéres</button>
                    <div class="dropdown-content">
                         <a href="{{ path('matiere_index') }}" >Liste des Matiéres </a>
                          {% if is_granted('ROLE_EDITOR') %}
                          <a href="{{ path('matiere_new') }}">Ajouter des Matiéres</a>
                          {% endif %} 
                      </div>
             </div>
        
            </li>
            </li>
            <li class="nav-item">
          <div class="dropdown">
               <button class="dropbtn">Rechercher</button>
                    <div class="dropdown-content">
                         <a href="{{ path('lister') }}" >rechercher par titre </a>
                          <a href="{{ path('article_par_prix') }}">rechercher par prix </a>
                           <a href="{{ path('lister1') }}">rechercher par Matiére </a>
                      </div>
             </div>
        
           </li> 
           <div class="nav-item">
           <a  href="{{ path('cart_index') }} " style="color:white;"> <i class="fas fa-shopping-cart mr-1"></i> Panier</a> <br>
             <a  href="{{ path('cart_index') }} " style="color:white;"> <i class="fas fa-address-book"></i> contact us </a>
                   </div>
                 <li>
           <div class="collapse navbar-collapse" id="mobile-nav">
            <div class="dropdown">
            <button class="dropbtn" > <img src="https://mdbootstrap.com/img/new/avatars/2.jpg" class="rounded-circle"   height="55"  alt=""   loading="lazy" /></i></button>
             <div class="dropdown-content">
               <a href="{{ path('security_login') }}">Connexion</a>
               <a href="{{ path('security_logout') }}">Déconnexion</a>
               {% if is_granted('ROLE_ADMIN') %}
               <a href="{{ path('admin_utilisateurs') }}">Administration</a>
               {% endif %} 
             </div>   

           </li>
      </ul>
    </div>
</nav>
    {% else %}
    <nav class="navbar navbar-expand-sm navbar-dark bg-primary mb-3">
          <img src="{{ asset('/uploads/test1.jpg') }}"  class="rounded-circle" width="100" hieght=-"100">

    <div class="container">
      <a href="/" class="navbar-brand">Céramique art daily</a>
         <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobile-nav">
          <span class="navbar-toggle-icon"></span>
         </button>
           <div class="collapse navbar-collapse" id="mobile-nav">
             <ul class="navbar-nav ml-auto">
                   <li class="nav-item">
                      <a href="{{ path('security_login')}}" class="nav-link">Connexion</a>
                  </li>
                   <li class="nav-item">
                      <a href="{{ path('inscri')}}" class="nav-link">inscription</a>
                    </li>
             </ul>
          </div>   
      </div>
     </nav>
    {% endif %}  
